{
  "name": "headwater-desktop",
  "productName": "Headwater",
  "desktopName": "social.pleroma.Headwater.desktop",
  "version": "0.0.1",
  "description": "Single-user social networking over encrypted email",
  "license": "Unlicense",
  "author": "Headwater contributors",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "packageManager": "pnpm@11.5.2",
  "engines": {
    "node": ">=24 <25",
    "pnpm": "11.5.2"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run",
    "build": "node --input-type=module -e \"import { rmSync } from 'node:fs'; rmSync('dist', { recursive: true, force: true })\" && tsc -p tsconfig.build.json",
    "stage:resources": "node scripts/stage-resources.mjs",
    "verify:resources": "node scripts/verify-resources.mjs",
    "package:dir": "electron-builder --dir",
    "package:mac": "electron-builder --mac dmg --arm64",
    "package:win": "electron-builder --win nsis --x64",
    "package:linux": "electron-builder --linux --x64",
    "dev": "electron .",
    "test:smoke": "pnpm --dir ../frontend build && pnpm --dir ../daemon build && pnpm build && pnpm run stage:resources && node scripts/smoke.mjs"
  },
  "dependencies": {
    "@deltachat/jsonrpc-client": "2.53.0",
    "@deltachat/stdio-rpc-server": "2.53.0",
    "@hono/node-server": "2.0.8",
    "hono": "4.12.27",
    "ws": "8.21.0"
  },
  "devDependencies": {
    "@electron/packager": "20.0.3",
    "@types/node": "24.13.3",
    "electron": "43.1.1",
    "electron-builder": "26.15.3",
    "typescript": "6.0.3",
    "vitest": "4.1.9"
  }
}
