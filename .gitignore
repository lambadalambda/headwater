node_modules/
dist/
# Delta Chat account databases and blobs
data/
# chatmail credentials — never commit
accounts.local.json
# local OAuth clients, sessions, and signing secret — never commit
*.auth.json
.env
.env.*
!**/.env.example
test-results/
.cache/
desktop/resources/
desktop/release/
